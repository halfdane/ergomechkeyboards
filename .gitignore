# temporary export targets (not docs/)
_site/
_static/

# generated PDFs
*.pdf

# direnv / nix
.direnv/

# puppeteer / node caches (if ever used)
.cache/
node_modules/

# docs/ is intentionally tracked (GitHub Pages source)
